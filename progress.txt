2026-07-28 performance implementation

DONE
- Diagnosed Safari paint/composite pressure, synchronous procedural bakes, and
  first-use WebGL program compilation from the supplied profiles.
- Moved world and cloud pixel generation to transferable worker jobs.
- Added stable mapped placeholders, async shader warmup, and reusable moon assets.
- Narrowed surface/system invalidation to parameters that affect those outputs.
- Added 30fps passive rendering, paused/offscreen suspension, DPR/MSAA controls,
  adaptive DPR fallback, and reduced DOM/compositor work.
- Added immutable share caching and aligned Vercel functions with the database region.
- Added deterministic bake tests.

VERIFIED
- bun run typecheck
- bun run lint
- bun run test: 68 passed
- bun run build
- bun run test:e2e: 13 passed, including worker-backed custom systems and idle pause

DEPLOYED
- Vercel production deployment: dpl_E3Aj8PLaJfS61jsLj8rbdnZpS4YR
- Production alias: https://little-worlds-delta.vercel.app
- Status: Ready; functions built in iad1
- Live smoke test: WebGL rendered 57,812 triangles and loaded bake.worker-B96fjZqW.js
- Immutable world response verified with one-year browser caching

MEASURED
- A/B compared revision 1755783 with production over three alternating runs,
  at DPR 2 and both native speed and 4x CPU stress.
- Native median interaction duration: 320 ms -> 40 ms (88% lower).
- Native median total long-task time: 4,736 ms -> 458 ms (90% lower).
- Native median main-thread task time: 8,046 ms -> 1,787 ms (78% lower).
- Orbit action completion plus observable system frame: 1,996 ms -> 592 ms (70% faster).
- Pause: 47 frames and 3,055 ms task time per 3 s -> zero observed work.
- Full methodology and limitations: PERFORMANCE_RESULTS.md

NEXT
- Re-run Safari and Chrome performance recordings against production to validate
  the hardware-level INP and frame-time targets.

2026-07-28 performance guardrails

DONE
- Added performance-budget.json for interaction, long-task, main-thread,
  renderer-idle, worker, console-error, entry-bundle, and worker-bundle limits.
- Added a repeatable headless Chromium benchmark with one-run smoke and
  three-run median modes.
- Added bundle and browser budget checkers, including optional same-runner
  regression comparison with a 10% limit.
- Added a Performance GitHub workflow for pull requests, main, nightly, and
  manual runs. Pull requests compare the candidate with their base revision.
- Added a pull request performance checklist.
- Added repository-specific performance requirements and commands to AGENTS.md.

VERIFIED
- Entry bundle: 234,202 B gzip <= 256,000 B.
- Bake worker: 90,901 B raw <= 102,400 B.
- Three-run median: first system frame 476.7 ms; interaction 32 ms;
  total/max long task 420 ms; main-thread task time 1,583 ms.
- Passive renderer 23.3 fps; paused frames and paused task time both zero.
- Worker and Event Timing instrumentation loaded; no browser errors.
- Relative budget comparison path passes with a same-result control.
- bun run typecheck
- bun run lint
- bun run test: 74 passed
- bun run build
- bun run test:e2e: 13 passed

NEXT
- Require the Performance workflow as a branch-protection check after its first
  successful GitHub run establishes that hosted-runner variance fits the budgets.
- Re-profile production on the original Safari hardware.

2026-07-28 per-browser galleries

CONTEXT
- The Worlds and Systems lists were global: GET /api/worlds and /api/systems
  selected the newest rows with no owner filter, so every visitor saw every world
  anyone had ever saved. Nothing in the schema recorded who saved what.

DONE
- Added a nullable owner_key column plus an (owner_key, created_at) index to both
  worlds and systems. Nullable only for pre-existing rows, which belong to nobody
  and are now reachable by their /w/:slug or /s/:slug link alone.
- Added src/lib/owner.ts: a random base64url key the browser issues to itself and
  keeps in localStorage, with an in-memory fallback for private browsing.
- listWorlds/listSystems/saveWorld/saveSystem now send it as x-owner-key. Fetching
  one thing by slug deliberately does not, so shared links still work for anyone
  and stay immutably cacheable.
- List handlers filter by the key, reject an ownerless POST with 400, and answer a
  keyless GET with an empty list.
- List responses moved from `public, s-maxage=10` to `private, no-store` with
  `vary: x-owner-key`. Deliberate: a shared CDN entry would serve one visitor's
  gallery to the next. Slug routes keep their one-year immutable caching, so the
  performance plan's share-link criterion is unaffected.
- Reworded the Worlds and Systems panel copy from "recently saved" to the
  visitor's own, and said that links still open for anyone.

VERIFIED
- bun run typecheck
- bun run lint
- vitest run: 74 passed, including 6 new owner-key tests
- playwright: gallery degradation and per-browser key isolation both pass

NEXT
- Run `bun run db:push` to add owner_key before deploying; the API writes it on
  every insert, so the column must exist first.
- Decide whether the anonymous key should later be claimable by a real account,
  which is the only way a gallery survives a new device or cleared site data.

2026-07-28 feature roadmap captured

CONTEXT
- New scope arrived as a wishlist across six themes: Solar Systems, Pop Culture,
  Exo Planets, Sculpting, Worlds, Universe. Organized into IMPLEMENTATION_PLAN.md
  as eight sequenced phases; the delivered performance plan moved below it.

PLANNED
1. Orbit-view display options: per-planet-coloured paths, moon paths, hide-paths
   toggle with hover reveal, labels toggle, moons on/off (performance lever).
2. Pluto from measured elements; Charon as a stretch goal.
3. Worlds pane: pick the target system when adding, warn before adding a
   duplicate world to a system.
4. Ancient worlds presets: Archean Earth, Proterozoic Earth, Noachian Mars,
   with Scan-tab chemistry to match.
5. Exoplanets: TRAPPIST-1 system plus famous singles; needs a new provenance
   label ("measured orbit, imagined surface") and possibly a cooler red dwarf.
6. Pop-culture homages (Star Wars, Project Hail Mary); original procedural
   interpretations only. Open question: binary-star support for Tatooine.
7. Animated fluids (water shimmer, band drift, lava) inside the existing
   passive-fps and pause budgets; flat vs detailed as an explicit quality
   choice for all planets.
8. Universe appearance attributes; start as a viewer preference, not saved
   system state.

NEXT
- Start phase 1 (display options) — smallest surface, and the moons toggle
  feeds the performance budgets directly.
- Every phase runs the local benchmark and Performance workflow against
  performance-budget.json before merge.

2026-07-28 phase 1: orbit-view display options

DONE
- Orbit paths now wear their planet's palette colour (the same tone the body
  falls back to before its bake lands), so line and planet read as one thing.
- Moons gained orbit paths in their own colours, in the single-world view —
  the only place moons render; the orbit view never built them.
- Display toggles: Orbit paths, Labels, Moons — chips on the Systems tab,
  persisted per browser in localStorage (little-worlds.display) with the same
  private-browsing fallback shape as the owner key.
- With paths hidden, hovering an orbit-view planet fades in that planet's
  path alone; it fades out on leave. Fade runs through the existing
  render-on-demand loop and respects pause/hidden/offscreen suspension.
- Labels are canvas-texture sprites with sizeAttenuation off (constant screen
  size), anchored above the body; renames redraw the small canvas only —
  names stay excluded from bake keys, and no toggle re-bakes anything.
- Moons off skips building them entirely — no meshes, no per-frame Kepler.
- The engine now publishes renderer line counts (canvas dataset.lines)
  alongside triangles, so tests assert path visibility truthfully.

VERIFIED (runs delegated to an Opus 5 agent)
- bun run typecheck; bun run lint.
- vitest: 80 passed, including 6 new display-preference tests.
- Build + size:check: entry 236,918 B gzip <= 256,000 (92.5%); worker
  90,901 B unchanged.
- Playwright: four new specs — hover reveal, labels add/remove geometry,
  moons toggle drops triangles and paths, display survives reload — pass
  repeatedly at 6 workers. One early failure was the labels spec trusting a
  fixed 2.5 s warm-up sleep; it now polls for a settled triangle count.
- perf:smoke: all 11 budget rows green (first system frame 360 ms,
  interaction 32 ms, long-task total 445 ms, main-thread 1,501 ms, paused
  work zero).

NEXT
- Add a benchmark scenario that exercises the moons toggle so its effect is
  recorded by the local benchmark, not only by e2e triangle counts.
- Two pre-existing sculpt-round-trip e2e tests sit within 0.4 s of the 30 s
  per-test cap under 6-worker contention and flip on loaded machines
  ("a system built from your own worlds renders in orbit" and its
  neighbour). The lever is the per-test timeout or worker count, not app code.
- perf smoke's activeRendererFps landed exactly on its 20 fps floor — zero
  headroom on a busier runner; worth a look before requiring the workflow as
  a branch-protection check.

2026-07-28 phase 2: Pluto

DONE
- Pluto is the Solar System's ninth body, measured everywhere it can be:
  a 39.482 AU, e 0.2488, i 17.16°, 247.94 y, 122.5° tilt, retrograde 6.39-day
  spin, and Charon at half its radius on the mutually locked 6.39-day orbit.
- No CC BY photographic map exists, so Pluto renders procedurally: new
  'pluto' palette running Cthulhu Macula tans up to Sputnik Planitia ice,
  under a genuinely blue haze.
- New identity rule realFor() in engine/planets.ts: a measured body is
  claimed by its photographic map, or — for texture-less Pluto — by a
  canonical seed recorded in REAL. Reseeding detaches it, exactly as the
  sculptor's existing texture rule. viewport.ts and scan.ts both use it.
- Scan tab: hand-written New Horizons profile (13 µbar N2 atmosphere,
  surface ices, tholins, water-ice bedrock, subsurface-ocean caveat).
  A reseeded pluto-preset world derives its scan as the ice family.
- README counts updated; systems/scale tests assert the odd orbit and the
  drawn Neptune-crossing in both scale models.

VERIFIED (full suite delegated to an Opus 5 agent)
- typecheck, lint, 82 unit tests, build.
- Bundle: entry 237,993 B gzip (93.0% of budget) — Pluto cost ~1.1 kB;
  worker 91,044 B (88.9%).
- perf:smoke all green across four runs; activeRendererFps rose from the
  20 fps floor to a steady 23.3 — the phase-1 zero-headroom flag is resolved.
  The ninth body's bake moved no metric measurably.
- Playwright: new Pluto spec passes (after fixing its own navigation bug —
  it asserted surface text without leaving the Atmosphere section). Also
  fixed a pre-existing ~15% flake: the duplicate-naming spec hardcoded
  "<name> II" while the app documents continuing a regnal line (Wimpond III
  -> Wimpond IV); the spec now follows the family. Both re-ran 6/6 green.
- The four display-options specs passed again; settledTriangles holds.

NEXT
- Phase 3: Worlds pane target-system picker and duplicate warning.
- The sculpt-round-trip e2e still runs within ~1.3 s of its 30 s cap under
  6-worker load; raise the per-test timeout when it next flips.

2026-07-28 phase 3: Worlds tab aims Add anywhere, duplicates warn

DONE
- WorldsPanel gained a destination picker: the system on the Systems tab
  (default, old behaviour) plus every system saved from this browser.
  Aiming at a saved system loads it, adds the world on an orbit outside
  everything there, and makes it the active system.
- worldInSystem() in lib/systems.ts: identity is serialize(sanitize(params))
  — the same object that gets saved and shared — so a renamed copy still
  counts, a reshaped one does not, and render-only fields are ignored.
- Duplicate adds warn inline (Add anyway / Cancel); allowed, never silent.
- Read-only destinations still become an editable copy, named up front.
- App loads both galleries on both tabs; select styled like text inputs.

VERIFIED (full suite delegated to an Opus 5 agent)
- typecheck, lint, 86 unit tests (4 new), build.
- Bundle: entry 238,434 B gzip (93.1%), +441 B for the phase; worker
  byte-identical to phase 2, as a UI-only phase should be.
- perf:smoke 3/3 all green; fps 23.3-26.7, no regression.
- e2e: a clean 20/20 full run. New picker spec and both phase-2 spec
  repairs passed both full runs. Run 1's two failures were fixed-sleep
  timing flakes under 6-worker contention; the agent instrumented first
  load directly and confirmed the galleries-on-both-tabs change adds zero
  requests to the first-load path (default tab is sculpt).

NEXT
- Three specs still rely on fixed waitForTimeout sleeps and fray under
  contention ("renders a planet on first load", the sculpt-round-trip
  orbit test, and the orbit-view render test); port them to the
  settledTriangles pattern when they next flip.
- First load fetches two Google Fonts from the network on the critical
  path; self-hosting the woff2 files would cut e2e variance and the
  suite's outbound-internet dependency.
- Phase 4 next: ancient worlds presets (Archean/Proterozoic Earth,
  Noachian Mars) with Scan-tab chemistry to match.

2026-07-28 phase 4: ancient worlds

DONE
- Three deep-time reconstructions load whole from an Ancient worlds chip
  row on the Sculpt tab: Archean Earth (anoxic, orange haze, iron-tinted
  global ocean), Proterozoic Earth (barren continents, ~1% oxygen),
  Noachian Mars (northern sea, rain, clays). Canonical seeds 3042/1042/4042.
- Honesty is structural: every profile opens with "Reconstructed:", and
  the gating reuses Pluto's canonical-seed rule — reseed and the world
  detaches into an ordinary member of its family (FAMILY map in scan.ts:
  pluto->ice, archean/proterozoic->temperate, noachian->desert).
- Biosignature verdicts tell the actual story: Archean is "Alive, but not
  advertising" (a caution about judging exoplanets by O2), Proterozoic
  "Faint, but certain", Noachian "Unknown — and worth the trip".
- typeOf/bodyDot/api describe() chains extended so gallery cards and
  system dots stay right; sanitize accepts the three new preset keys.

VERIFIED (full suite delegated to an Opus 5 agent)
- Cleanest phase yet: typecheck, lint, 93 unit tests (7 new in
  scan.test.ts), build, both bundle budgets, 21/21 e2e with zero flakes,
  perf all green across three runs, fps steady at 23.3.
- Agent measured first load directly: the chip row adds ~27 ms to a
  ~1000 ms mean — noise. Nothing touches the bake path.

FLAGGED
- Bundle trajectory: the reconstruction prose cost 3,234 B gzip — 1.26%
  of the entry budget, more than phases 2 and 3 combined. Entry is now
  241,668 B (94.4% of budget, 14.3 kB headroom). Profile text is only
  read after a spectrometer run, so if more prose-heavy collections come
  (exoplanets, pop culture), split the profiles out of the entry chunk
  with a dynamic import before writing them.
- Sculpt round-trip e2e passed at 27.7 s of its 30 s cap — a good draw,
  not a fix.

NEXT
- Phase 5: exoplanets — needs the "measured orbit, imagined surface"
  provenance label and possibly a cooler red dwarf star kind.
- Consider the profile-text code split before phase 5/6 add more prose.

2026-07-28 phase 5: observed systems, compact-system engine, bundle split

DONE
- Four observed systems: TRAPPIST-1 (all seven planets, measured a/P/e/
  radius, tidally locked days), Proxima Centauri, 51 Pegasi, Kepler-452.
  New 'observed' origin says it plainly: orbits measured, surfaces
  imagined, nobody has seen one. Ember dwarf star kind (0.1 M☉); A_MIN
  0.04 -> 0.01 so duplicated compact systems sanitise intact; sub-10-day
  years quoted to one decimal ("1.5 day year", not "2").
- Compact-system engine adaptation (scale.ts): systemStretch fills the
  frame for systems inside 1 AU; tempoFor slows the clock until the
  fastest orbit takes 0.2 drawn years. One factor each per system, so
  internal ratios stay exact; Solar System provably untouched (both 1).
- Phase-4 bundle flag closed: REAL_PROFILES + ANCIENT_PROFILES moved to
  data/profiles.ts behind a dynamic import inside computeScan (now
  async, awaited during the 2.1 s sweep). Entry 234,294 B gzip (91.5%),
  below the phase-1 baseline; profiles chunk 10.2 kB gzip.
- Size guard blind spot closed: check-bundle-size.mjs now budgets every
  lazily-fetched chunk (16 kB gzip each) — split code cannot grow
  unwatched again.
- The two sculpt-round-trip specs marked test.slow(): 4 failures in 9
  full parallel runs while finishing within a second of the default cap
  was contention, not defect; the tripled cap ends the coin flips.

VERIFIED (full suite delegated to an Opus 5 agent)
- typecheck, lint, 99 unit tests (6 new), build (43 modules), both
  bundle budgets + new lazy budget, e2e 22/22 on the confirming run,
  perf green across six smoke runs at fps 23.3.
- The agent nearly called a perf regression on first read: post-e2e hot
  runs showed +31% firstSystemFrame. Settled re-runs landed exactly on
  the phase-4 baseline — machine state, not code. Worth remembering:
  longTaskMax reached 469/500 ms hot; perf:ci runs cool, but a throttled
  shared runner would trip that row first.
- bun run db:push against the migrated Neon database: "No changes
  detected" — schema in sync, owner_key migration confirmed live.

NEXT
- Phase 6: pop-culture worlds — the binary-star question (Tatooine)
  must be decided before scoping.
- Google Fonts on the first-load critical path remains open.

2026-07-28 phase 6: pop-culture worlds — homage systems, labelled fiction

DONE
- Decided: single-star scope. Tatooine orbits Tatoo I alone, and its
  scan note owes the fiction its second sun out loud ("This engine
  draws one, and owes it the other"). Binary stars remain a possible
  future engine task, not a blocker.
- Four homage systems, all origin 'imagined': Outer Rim (Mustafar,
  Tatooine, Hoth), 40 Eridani (Erid) and Tau Ceti (Adrian) from
  Project Hail Mary on real star masses, Alpha Centauri A for Avatar —
  Polyphemus (gasMist giant) plus Pandora, added at the user's request.
  Pandora is really Polyphemus's moon and moons only render in the
  single-world view, so it rides its own orbit and the caption says so.
- Six new presets with palettes and canonical seeds (tatooine 1977,
  hoth 1980, mustafar 2005, erid 2021, adrian 1021, pandora 2009);
  same identity rule as Pluto/ancient: reseed detaches into the FAMILY
  fallback (desert/ice/lava/desert/lava/temperate).
- FICTION_PROFILES in data/fiction.ts, every atmoTitle opening
  "Fiction:" — Erid at the novel's 29 bar with an unobservable surface,
  Adrian's biosignature the Petrova line rather than a pigment, Pandora
  with the xenon that makes rich air unbreathable (new Xe species).
  Loaded via a conditional dynamic import inside computeScan: its own
  5.1 kB gzip chunk, fetched only when a story world actually scans.
- Opus finding fixed before commit: Pandora at 1.6 AU interpenetrated
  Polyphemus in same-size mode (drawn gap 0.236 vs the 0.480 two
  0.24-radius discs need — ~2 s of clipping per minute of viewing).
  Moved to 2.2 AU (gap 0.592, and the scale-mode 1.01x graze widens to
  comfortable); regression unit test pins the gap via sameDist itself.

VERIFIED (full suite delegated to the same Opus 5 agent)
- typecheck, lint, 109 unit tests (10 new), build (44 modules), all
  four bundle budgets (entry 235,934 gzip 92.2%; fiction chunk 31.2%),
  e2e 24/24 (one aim-flake rerun clean, see NEXT), perf 8/8 green at
  fps 23.3, bun run db:push "No changes detected".
- Compact-system engine confirmed on the new systems: 40 Eridani
  stretch 23.85 / tempo 1.74, Tau Ceti 20.80 / 1.42, Solar System
  exactly 1/1. Fiction chunk confirmed absent from the network until a
  story world scans. All five homage/observed systems survive
  duplicate + re-sanitise with orbits intact.

NEXT
- longTaskMax creep: 486/500 ms hot this session (469 last phase).
  perf:ci runs cool, but this trips first on a throttled runner —
  consider profiling the bake warmup before phase 7's animated fluids.
- First-ever flake of the hover-reveal spec (pointer sweep missed all
  frozen planets; 8/8 in isolation). Aim-based fragility, fifth member
  of the fragile-test family — worth a deterministic aiming helper.
- Google Fonts on the first-load critical path remains open.

2026-07-28 phase 7: fluids that move, and a flat/detailed rendering choice

DONE
- Fluid motion, all on rotation's clock (this.t): water shimmer via a
  time-driven normal perturbation injected into the shell material
  (onBeforeCompile, customProgramCacheKey — exactly one program
  variant, startup-warmed); lava (emissive palettes) ripples slow and
  heavy and pulses emissiveIntensity; sculpted gas giants reach the
  GAS_FRAG animation (differential band drift + storm vortex, swirl by
  roughness) through the flat tier. Paused/hidden/offscreen freeze all
  of it because t freezes — no new machinery.
- Rendering tiers as a persisted display choice (Auto/Flat/Detailed
  chips on Sculpt; DisplayOptions.tier, migration-safe): flat = the
  bake worker's orbit-view map on a smooth sphere (palette mid-tone
  placeholder, last-bake-over-grey-flash, flatBakeId latest-wins);
  detailed = displaced terrain + shells. Auto: photos and gas flat,
  sculpted rock detailed. A photo planet forced detailed renders the
  procedural interpretation of its own params; tier never enters
  identity (sanitize drops it — pinned by unit test).
- The twice-flagged hover-spec aim flake fixed: staggered finer second
  sweep pass. The pause spec's setup poll widened to 20 s after Opus
  caught it at 9/10 frames under contention.

VERIFIED (full suite delegated to the same Opus 5 agent)
- typecheck, lint, 113 unit (4 new), build (45 modules), size:check
  (entry 237,150 gzip 92.6%, worker/lazy chunks byte-identical), e2e
  26/26 on the confirming run, perf 5/5 green with paused rows 0
  everywhere, db:push "No changes detected".
- The two silent-failure risks probed directly: the shader anchor
  really matched (compiled GLSL contains the perturb block exactly
  once across 18 startup shaders), and paused really is frozen
  (canvas pixels byte-identical 1.6 s apart; pixels differ while
  running). Texture ownership across photo->flat->photo restores
  Jupiter's exact 19,156 triangles with zero console errors; slider-
  hammered in-flight bakes settle clean; Saturn/Jupiter unaffected
  after sculpted-gas visits (uFlow reset unconditional).

NEXT
- e2e suite wall time grew 49 s -> ~1.1 m (2 new specs + finer sweep,
  but per-test times rose broadly); the fixed-sleep specs have less
  slack. Worth a pass converting remaining fixed sleeps to polls.
- activeRendererFps touched its 20 floor once in five runs (first time
  since phase 1); longTaskMax hot ceiling 470/500. Both rows to watch
  before phase 8 adds starfield/nebula work.
- Google Fonts on the first-load critical path remains open.
